{
  "name": "the-middle-party-game",
  "version": "1.0.0",
  "private": true,
  "description": "A responsive room-code social spectrum game for a shared host screen and player phones.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.1"
  }
}
